name: studio_musicale_app
description: App di amministrazione studio musicale.
version: 1.0.0+1

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0  # <--- ABBIAMO AGGIUNTO QUESTA

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
